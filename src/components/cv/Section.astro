---
const { title, class: className } = Astro.props;
---

<section class={`max-w-[700px] mt-0 mr-auto ml-auto mb-12 ${className}`}>
    {title && <h2 class="mb-2 font-bold leading-normal text-base">{title}</h2>}
    <slot />
</section>
